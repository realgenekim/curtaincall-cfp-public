# Plan: close to 100 — full sbek rubric coverage + every bonus

*Written 2026-08-10 after the overnight-agent review
(logs/2026-08-10-overnight-agent-review.md). Baseline standing ~45–50/100
with scoring unlocked. This plan enumerates EVERYTHING between here and
100% on the 96-item rubric plus all brief bonuses. Deadline: Wed Aug 12,
10PM PT. Rubric authority: docs/research/rubric-gap-analysis-2026-08-09.md.*

## Standing rules (unchanged, binding)

CLAUDE.md law; every mutation a domain verb appending a fact; jsonl sandbox
for agents, prod only for the operator; nREPL-only compile checks;
`bin/kaocha unit` full-run green before any commit; the views/ split is now
LAW (view_architecture_test enforces it) — new view code goes in the right
views/* namespace. The judge scores SCREENS and literal words.

## Phase 0 — Morning operator (human + main session, ~2h) — BLOCKS EVERYTHING

1. Telemetry DDL as table owner (REPL; table + append-only triggers +
   INSERT,SELECT grant). Verify: N curls → count grows by N.
2. Session-cookie secret into Secret Manager; verify runtime load.
3. Verify the Speaker persona ("Amara Devlin") is AIE-ONLY; if she holds
   any other-event seat, swap the persona to a clean speaker.
4. `DEMO_PERSONAS=on` + `DEMO_PERSONA_EMAILS` on swyx-cfp-saas-killer
   ONLY; verify curtaincallcfp grows no buttons.
5. **The decision wave** (staged clock, domain verbs): ~60 Accepted →
   informed → scheduled into 3–4 created rooms; ~25 Waitlisted; ~40
   Declined+informed. Without it every public widget renders empty.
6. Deploy; then the full acceptance drive from Package A of the judge-
   readiness plan (personas, recycle-survival, echo roundtrip, tenancy).

## Phase 1 — Abstract Management (bead 7wy; ~8–12h; area 20 @ 13% → target 90%+)

The biggest remaining prize. Items ABS-01..14:
- **Reviewer assignment as OPTIONAL scope** (ABS-05 w3 scoping, "the
  single highest-value item we fail"): assign submissions to named members;
  committee default stays :all (doctrine #1 intact — assignment narrows,
  never gates visibility unless blind mode). Assignment facts; an
  "Assigned to you" filter that IS the reviewer queue.
- **Reviewer-only dashboard** (CFP-10 w2 scoping): a reviewer landing
  distinct from organizer nav (non-chair members get the review-first rail).
- **Scorecard with named criteria** (ABS-03 w3, ABS-04 w1): per-event
  criteria (numeric/dropdown/free-text, optional weights) configured on a
  screen; reviewer fills per-criterion; aggregate on the board; the
  existing 1–5 stars becomes the default single-criterion scorecard.
- **Review rounds** (ABS-01 w3, ABS-02 w2): two named rounds with dates,
  per-round pool + scorecard; round advance is a fact.
- **Blind review toggle** (ABS-07 w2): per-event; hides speaker
  name/org/bio in reviewer views; organizer always sees.
- **Per-reviewer progress + nudge** (ABS-08 w2, ABS-09 w1): assigned/done
  counts per member; select laggards → drafted reminder into comms.
- **Review-scores CSV export** (ABS-13 w2): one route off the board.
- **Multi-speaker add control** (ABS-11 w2): "add another speaker" on the
  public form with role labels (verify — may exist).
- **Conflict-of-interest recusal** (ABS-12 w1): "recuse myself" on a
  submission; recused reviewers drop from that sub's coverage denominator.
- ABS-14 (AI review): claim none — judged n/a, neutral. Skip.

## Phase 2 — Speaker ops + files (beads n1b + 7nr; ~12–16h; two agents, disjoint)

**F — Speaker ops** (SPK-01..06/09/12/13/14/15/16, CNT-07/08):
roster page (search/filter/status/add/edit) + speakers.csv import +
speaker-level status (Invited/Confirmed) + portal-invite action + tasks
with due dates + timezone + required flag + Complete/Incomplete chips +
deliverables dashboard (per-speaker per-task grid) + bulk reminder +
group compose with tokenized templates ({speaker_name}, {talk_title})
showing a fully-resolved per-recipient preview + custom person fields +
automated reminder schedule (a fact + a comms render is enough).
Seed the two must-have task types: hotel-stay form, flight-reimbursement
form (swyx Q5).

**G — Files** (CNT-01/02/04/05/06/13/14, SPK-08/10):
file-request task type with instructions + due; multipart upload (GCS
bucket exists — snapshot bucket pattern; local-disk fallback in dev);
typed files (Presentation/Poster/Handout) listed on the session; versions
with expandable history (CNT-04 rule: "v2" with v1 beneath); two-sided
comment thread on a file; stated upload constraints; central library
page; ZIP export; headshot UPLOAD path on the portal profile (SPK-08:
the fixture hands the agent headshot.png — an upload control must exist
even though pool-face covers display).

## Phase 3 — Widgets completion + agenda extras (~6–8h)

- Verify/complete Package D: gallery grid (EMB-12), Track facet chips +
  search semantics (EMB-02/03: list+itinerary match titles AND speaker
  names; gallery names only), speaker directory search (EMB-04),
  cross-surface consistency test (EMB-16), itinerary card anatomy (EMB-09).
- Session detail page completeness (EMB-08): tabs "Session Details /
  Subsessions (0)", full start–end, Format line.
- **Personal schedule** (EMB-10/11): star sessions (server-side per
  session-cookie), "My schedule" view, per-selection .ics.
- **Embed builder** (EMB-15 w2 handoff): a builder page (widget type +
  format picker + copyable script/link snippet). Ship the builder + a
  working iframe/link embed; the custom-element loader is stretch.
- **Auto-schedule** (AIA-08 w1): a "Suggest schedule" button — greedy
  conflict-aware pack of the unplaced tray. Cheapest honest "AI agenda".
- **Organizer content editing + history** (CNT-09/10/11): session
  title/abstract edit + speaker-profile edit on organizer screens (verbs
  exist — need SCREENS), per-object history panel from the log (we ARE an
  event store — render the facts) + restore-as-new-fact.

## Phase 4 — Bonus sweep (bead 973; ~4–6h)

- **Airtable one-way sink** (brief bonus): sinks registry entry mirroring
  submissions/decisions to an Airtable base on write; fix bead sjp
  (relative URL). Read-mostly per swyx.
- **Resend email** (swyx Q6 "should work on an MVP basis"): comms sender
  behind an env key on the demo service; outbox stays the log of record.
- **/api/v1 polish**: efa trailing-slash fix; docs page linked from
  Exports; verify all endpoints against the anti-sessionize log's list.
- **MCP endpoint per event** (platform-probe parity): /events/:slug/mcp
  serving the exports as tools. Stretch.
- **Forge mirror** (teeny bonus): mirror the allowlist repo to
  forge.smol.ai.
- **Trajectory bonus** (speculative): plans/ + logs/ captain's logs are
  already the trajectory — include them in the public repo.
- **Cloudflare: SKIPPED** (ratified — migration risk > mild bonus; speed
  numbers are the substitute story).

## Phase 5 — Speaker CRM extra credit (bead ncu; +10 area; ~6–8h; LAST)

Only after phases 0–4: /people cross-event directory (CRM-01 anchors),
attribute filters, person detail (activity from the log + internal notes),
tags, CSV import, duplicate detection (email match), push-into-event,
outreach composer, mini dashboard. The log already holds every activity —
most of this area is projections + screens.

## Phase 6 — Submission logistics (bead crf; Gene + main session)

Allowlist public repo (privacy fence — NEVER this repo) with trajectory
artifacts · walkthrough video on the deployed site · the submission form ·
pick THE service URL (bead 3as: retire one) · **nuk rehearsals (Ann
first)** · DM swyx the URL + persona credentials for the free calibration
run (Discord: he offered).

## Budget honesty

Phases 0–4 ≈ 32–44 agent-hours + operator time — feasible by Wednesday
with 2–3 parallel agents on disjoint namespaces (the views/ split makes
this clean). Phase 5 only if 0–4 are green. Re-run the sbek-style
walkthrough after each phase lands on the deployed demo; the score, not
the commit count, is the meter.

## Unit sequencing (added after overnight calibration)

One UNIT = what one overnight agent session demonstrably delivers (~14
planned hours: last night's A+B+C+partial-D+views-split, 8,845 lines,
suite green). Distance to done:

- **Unit 1 = Phase 1** (Abstract Management, whole area).
- **Unit 2 = Phase 2** (speaker ops + files; splittable into two parallel
  agents on disjoint namespaces if wall-clock matters more than agents).
- **Unit 3 = Phases 3+4** (widget completion + bonus sweep).
- **Unit 4 (optional) = Phase 5** (CRM +10) + spillover from 1–3.

Phase 0 is operator work (not a unit) and gates judge visibility of
everything. Phase 6 is Gene's. After EVERY unit merges: full suite green,
review by the main session, and a live walkthrough on the deployed demo —
the score is the meter, not the commit count.

## Unit 0.5 (added, Gene-ratified) — dismantle server.clj before parallelizing

Prelude bead: the server.clj split (see bead for spec). Rationale: the
views/ split is what makes parallel units safe; server.clj (3,251 lines)
is the last file all three units must touch. Extract handlers by domain to
handlers/*.clj + one thin route table + middleware.clj, enforced by an
architecture test; split store.clj engine from folds.clj the same pass.
Mechanical, zero behavior change, suite green before/after. Run BEFORE
Unit 1, or as the first act of Unit 1's session.
